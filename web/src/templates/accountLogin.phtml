<?php include "header.phtml" ?>
<?php if (!$loggedIn): ?>
    <h1>Login</h1>
    <form method="POST">
        <table>
            <tr>
                <td><label>Username</label></td>
                <td><input placeholder="Enter Username" name="username" value="<?= $username ? $username : '' ?>" required></td>
            </tr>
            <tr>
                <td><label>Password</label></td>
                <td><input type="password" placeholder="Enter Password" name="password" required></td>
            </tr>
            <tr>
                <td></td>
                <td><button name="login">Login</button></td>
            </tr>
        </table>
    </form>
    <?= $username ? '<p>Invalid username and/or password.</p>' : '' ?>
<?php else: ?>
    <h1>Logged in</h1>
    <p>Welcome <?= $username ?>!</p>
    <?php if ($username == 'admin') {
        include "buttonAccountList.phtml";
    }
    else {
        include "buttonTransferList.phtml";
    }?>
<?php endif; ?>
<?php include "footer.phtml" ?>
